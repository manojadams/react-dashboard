import styled from "@emotion/styled";
import Logo from "../Logo";

function Header() {
    return (
        <Wrapper>
            <Logo />

        </Wrapper>
    )
}

const Wrapper = styled.div`

`;

export default Header;
